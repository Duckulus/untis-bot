{
  "name": "untis-bot",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "start": "yarn prisma:gen && turbo run start",
    "deploy": "yarn db generate && yarn start",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "prisma:gen": "yarn workspace db prisma db push",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "eslint-config-custom": "*",
    "prettier": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "dependencies": {
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19"
}